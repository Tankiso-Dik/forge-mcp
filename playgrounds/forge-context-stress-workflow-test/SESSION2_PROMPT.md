Resume the prior session in this Forge-managed workspace.

This is Session 2.

Your job:

1. orient yourself from current workspace state
2. produce `ARCHITECTURE_PLAN.md`
3. produce `RISK_REGISTER.md`
4. tighten `PRODUCT_SPEC.md` only if the architecture work clearly requires it
5. make the portability posture explicit in architecture and risks instead of leaving it implied
6. do not start the executive summary yet

Operating expectations:

- Resume from current continuity instead of rediscovering the whole workspace from scratch.
- Use explicit issue handling if an unresolved contradiction materially affects architecture.
- Keep terminal narration brief.
- Avoid low-value notes; only write continuity state that helps later work.
- If a source file is still pulling the design in two directions, make the disagreement visible rather than smoothing it away.

Success means:

- architecture and risk documents are real, not generic
- the documents are aligned with the product direction so far
- the portability and trust posture is explicit, not fuzzy
- the next session can handle change without needing to rediscover prior decisions
